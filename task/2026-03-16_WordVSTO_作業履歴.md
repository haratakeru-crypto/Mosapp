### 今日の作業履歴（2026‑03‑16）

- **3-2（セクション区切り：次のページから開始）の VSTO ログ対応**
  - `MOSWordVSTOAddIn` / `New_MOSWordVSTOAddIn` の `Ribbon.xml` で、idMso `InsertSectionBreakNextPage` が環境依存でエラーになる問題を確認。
  - `Ribbon.xml` の 3‑2 用 idMso を `SectionBreakInsert` に変更し、`Ribbon.cs` の `CommandOnAction` で  
    `SectionBreakInsert` 実行時に `Logger.LogCommand("InsertSectionBreakNextPage")` を書くよう変換。
  - さらに確実にするため、`New_MOSWordVSTOAddIn/ThisAddIn.cs` にポーリング処理を追加し、`wdSectionNewPage` のセクション数が増えた場合に `InsertSectionBreakNextPage` をログするよう実装。
  - `WordChecker1_3.CheckTask_1_3_02` のデバッグログ（H3_2）から、初回は `logOk:false` だが、その後は `fileStateOk:true, logOk:true, result:true` になっていることを確認し、3‑2 が正しく〇判定されるようになったことを確認。

- **3-5（段区切り）の判定ロジック改善とログ対応**
  - `WordChecker1_3.CheckTask_1_3_05` を確認し、「●茨城県天心記念五浦美術館」の直前に Chr(14) または `^n` があるかで段区切りを検出していることを整理。
  - この判定に VSTO ログも組み合わせるため、`CheckTask_1_3_05` の末尾を  
    `fileOk`（ファイル状態）＋ `logOk = LogReader.HasCommandExecuted("ColumnBreak")` の AND 判定（`finalResult = fileOk && logOk`）に変更。
  - 同時にデバッグログ H3_5 に `fileOk/logOk/result` を出力するよう変更。
  - `New_MOSWordVSTOAddIn/ThisAddIn.cs` の `ShowAllPoll_Tick` に、文書全体の Chr(14) 数をカウントし、増えた時に `Logger.LogCommand("ColumnBreak")` を出す処理を追加（段区切り操作をログとして検知）。
  - `ColumnBreak` を idMso で直接フックしようと試みたが、環境で「不明な Office コントロール ID」エラーになったため、`Ribbon.xml` から `ColumnBreak` の `<command>` 定義は削除し、**Chr(14) ポーリング方式のみ残す**構成に戻した。
  - VSTO アドイン（特に `New_MOSWordVSTOAddIn`）はその都度 `Rebuild-And-Install-WordVSTO.ps1` で再ビルド＆再インストールし、Word 側で反映されていることを確認。

- **ログ・デバッグまわり**
  - `WordChecker1_3.cs` の H3_2/H3_5/H3_6 向けのデバッグログ（`WriteDebugLog13`）を活用し、  
    `debug-340a5e.log` を使って、各タスクの `fileStateOk` / `logOk` / `result` / `anyOk` の状態を確認。
  - H3_2 のログから、3‑2 は VSTO ログ導入後に安定して `result:true` になっていることを確認。
  - H3_6（3‑6 行間 1.3）は `anyOk:false` が続いていることを確認し、まだ判定が厳しめである可能性を把握（今回は修正まで未実施）。

---

### 次回やること（TODO）

1. **3‑2 の初期状態が〇になってしまう問題の確認**
   - 現状：H3_2 のログでは `fileStateOk:true, logOk:true, result:true` が続いているが、「初期状態（操作前）でも〇になる」疑いがある。
   - 確認ポイント:
     - 初期文書を開いた直後に 3‑2 を採点し、`debug-340a5e.log` の H3_2 行で `fileStateOk/logOk/result` がどうなっているか確認する。
     - `InsertSectionBreakNextPage` ログが「初期状態で既に存在してしまっている」のか、それとも `fileStateOk` のみで通過しているのかを切り分ける。
   - 必要なら:
     - 「採点開始時に VSTO ログを必ずクリア」するタイミング（既に `LogReader.ClearLog` を呼んでいるか）の再確認。
     - 3‑2 について、「ログ必須」条件（`fileStateOk && logOk`）が初期状態で満たされないように、ログリセットや判定条件の微調整を行う。

2. **段区切り（3‑5）が正しく判定できていない問題の詳細調査**
   - 現状：「段区切りがうまくいっていないかも」という状態。  
     → どちら側で落ちているか（`fileOk` か `logOk` か）を、H3_5 のログで確認する。
   - 具体的な次回タスク:
     - プロジェクト3のタスク5（3‑5）を**初期状態→操作→採点**の流れで実施し、それぞれの段階で:
       - `%TEMP%\mos_word_log.txt` に `ColumnBreak` が出ているか。
       - `debug-340a5e.log` の H3_5 行の `fileOk/logOk/result` がどう変化するか。
     - もし `fileOk:false, logOk:true` の場合は、Chr(14) / `^n` の検出ロジックやチェック範囲（`MoveStart(-2)` など）の見直しを行う。
     - もし `fileOk:true, logOk:false` の場合は、Chr(14) カウントのポーリングがうまく増加を検知できていない可能性があるため、
       - ポーリングのタイミングや対象範囲（`doc.Content.Text`）の取得タイミング（段区切り直後に更新されているか）を追加ログで確認し、必要に応じてロジックを修正する。

3. **（余力があれば）3‑6 行間 1.3（H3_6）の許容範囲の再調整**
   - H3_6 のログで `anyOk:false` が続いているため、行間判定が厳しすぎる可能性あり。
   - 行間倍数・ポイント値の許容範囲が適切か、また段の取得範囲が問題仕様に合っているかを確認・微調整する。

