# 2026-03-30 作業まとめ（MOS Word app）

## 今日やったこと

- `WordChecker1_4.CheckTask_1_4_06` のページ罫線「囲む」判定について、`囲む=影` が誤って正解になる件を再現し、`debug-6b16c7.log` で実値を比較して原因を特定。
- COM 側で取得できる値の差分として `bordersShadow`（影/Boxの切替に相当）をログに出し、`囲む（影）` は `result=false`、`囲む（Box）` は `result=true` となることをログで確認。
- 判定ロジックを `bordersShadowOk`（Boxのみ通す）に更新し、影側を不正解へ落とす修正を実施。
- 併せて COM で `Art/Shadow/Surround` が取れない環境もあったため、`raw値` や `document.Shapes` の簡易サンプル等もログ出しして追加の切り分けを行った。

## 次回行うタスク

1. プロジェクト4-6の「囲む」だけが正解になるようにする。（現在は３Dも正解になってしまう）
   →プロジェクト4-6の問題内容では、線の太さや種類で3Dを満たしていないため、システム上でOKを押した瞬間に囲むになる
   →そのため、3Dも正解になってしまう挙動となっていた
   →厳密に判定するためには、VSTOを使ってUIの操作を再現して確認する必要がある（コスト高）
2. プロジェクト4のコメントの採点を行うとWord.docxが落ちてしまう
3. プロジェクト4-3が初期状態で正解になる場合があるか確認

