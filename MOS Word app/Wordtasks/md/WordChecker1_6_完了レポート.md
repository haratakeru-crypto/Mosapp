# プロジェクト6 採点機能 完了レポート

プロジェクト6（表操作）における全7タスクの採点機能について、見出し直後の表ブロックを特定する「ターゲット特定ロジック（GetTableXmlNearText）」の導入、および `TableSplitTable` や `TableSplitCells` といったフック不可能なコマンドへのユニーク状態検知の適用、さらに列幅均等化（`TableColumnsDistribute`）への操作ログAND判定の実装など、堅牢な判定ロジックの実装が完了しました。

すべてのタスクについて実機検証が行われ、**「正しい操作のみを完全に合格（〇）とし、誤った操作や曖昧な操作は厳密に不合格（✖）とする」** 高度な採点精度を達成しています。

また、本プロジェクトで蓄積された数々の技術的ナレッジは、後続のすべてのプロジェクト（プロジェクト7〜10）の品質の礎となりました。

---

## 採点ロジック改修成果の一覧

| タスク | 要件 | 旧ロジックの問題点 | 改修後のロジック（本実装） | 手動検証 |
| :--- | :--- | :--- | :--- | :---: |
| **6-1** | 文字列を表に変換 | COMでの表走査によるクラッシュ・リークの懸念。 | XML解析（OOXML）。行数・列数に加え、`tblW` および `tcW` の `w:type="auto"` （自動調整）を確認。 | **〇 (合格)** |
| **6-2** | 表の分割 | `TableSplitTable` は Ribbon フック不可能。表の特定が曖昧。 | XML解析。1つ目の表の行数（4行）と、直後（1000文字以内）に2つ目の表が開始されているかを構造的に検出。 | **〇 (合格)** |
| **6-3** | セルの分割 | `TableSplitCells` は Ribbon フック不可能。セルの結合・分割の判定がルーズで誤学習の危険性。 | XML解析。1つ目の表を特定し、1行目＝4セルかつ他行＝2セルの構造（`gridSpan` 活用）を厳格検証。 | **〇 (合格)** |
| **6-4** | 表に特定の書式（効果）を適用 | 他のアート効果や単なる文字色変更との区別ができず誤判定するリスク。 | XML解析。「北海道のうまいもの市場売上」直後の表の1行目で、`<w14:textOutline>` 内に accent2/ED7D31、塗りも同色、影/反射/光彩なしを厳密検出。 | **〇 (合格)** |
| **6-5** | 列の幅を揃える | 手動でチマチマ幅を揃えるような誤学習・ズル操作を弾けない。 | **XML + ログ AND**。全 `<w:gridCol w:w="...">` が誤差20twip以内で同一値、かつ `TableColumnsDistribute` ボタンのフックログあり。 | **〇 (合格)** |
| **6-6** | タイトル行の繰り返しを設定 | 全表ループによるCOMリーク、および位置の不特定。 | XML解析。「北海道のうまいもの市場売上」直後の表の1行目 `<w:tr>` 内の `<w:trPr>` に `<w:tblHeader>` タグがあるかをピンポイント検出。 | **〇 (合格)** |
| **6-7** | 表の作成とヘッダー | COMのままで安定しているか不安。 | **COMのまま（現状維持）**。行数13・列数3・1行目「最高売上」「平均売上」「合計売上」の条件が極めてユニークなため、改修によるバグ混入リスクを避けて最適化。 | **〇 (合格)** |

---

## 特筆すべき技術的アプローチと解決策

### 1. 見出し近傍の表ブロックを一意に特定するGetTableXmlNearText
文書内に複数の表がある場合、従来の全表ループ判定では、目的ではない無関係な表に対してユーザーが行った操作を「正解」と判定してしまう恐れがありました。
本実装では、表の直前にある見出しテキスト（例:「北海道のうまいもの市場売上」）をフックに、その付近にある表ブロックのみをピンポイントで切り出す `GetTableXmlNearText` を開発。これにより、誤判定を完全に排除しました。

### 2. idMsoでフックできないコマンド（表分割・セル分割）の構造的ユニークシグネチャ判定
Wordの仕様上、表の分割（`TableSplitTable`）やセルの分割（`TableSplitCells`）は、リボンのコマンドID（`idMso`）をフックしてもログが記録されません。
これに対してはログに頼らず、XML上で表が構造的に分割されているか（1つ目の表が4行で終わっているか）、またセル分割が行われて「1行目だけ4セル、他は2セル（`gridSpan`）」という特徴的な構造になっているかという、XML上の「ユニークシグネチャ」を捉えることで解決しました。

### 3. 「手動幅合わせ」のズルを弾くXML＋ログのAND判定
タスク6-5の「列の幅を揃える」は、ユーザーが定規を見ながら手作業で各列をチマチマ移動して「同じ幅」に設定した場合も、最終XML上は綺麗に同じ値になってしまいます。
しかし、これは「列幅を揃えるボタン」の学習価値を無視した誤学習です。そこで、XMLの均一幅判定に加え、アドインからフックした `TableColumnsDistribute` コマンドログが存在することを「AND条件」とすることで、機能の正しい使用プロセスを保証しました。

---

## 💡 C#およびVSTOアドイン開発における重要な技術的知見・注意点

> [!IMPORTANT]
> 以下のノウハウは、他AIが残した数々の深刻なバグやOfficeの奇妙な挙動を解析し、実機検証を重ねて得られた極めて貴重な設計遺産です。

### 技術リファレンス（XMLタグ名）
- **表の繰り返し設定**: `<w:tblHeader />` （`<w:trPr>` 内に存在）
- **列幅の均等化**: `<w:tblGrid>` 内の `<w:gridCol w:w="..." />` の値を比較。
- **文字列の幅に合わせる**: `<w:tcW w:type="auto" />` （セル単位）および `<w:tblW w:type="auto" />` （表単位）。
- **セルの分割・結合**: `<w:gridSpan w:val="数値" />`。
- **文字の輪郭・塗り**: `<w14:textOutline>`, `<w14:textFill>`（`<w:rPr>` 内）。配下に `<w14:schemeClr w14:val="accent2">` や `<w14:srgbClr w14:val="ED7D31">` などで色を表現。
- **影・反射・光彩**: `<w14:shadow>`, `<w14:reflection>`, `<w14:glow>`（`<w:rPr>` 内）。

### Regex 関連の注意点
- `Regex` を使用する際は、`System.Text.RegularExpressions.RegexOptions.Singleline` を指定して、タグが複数行にまたがっていてもマッチするようにすること。
- 内容（テキスト）をチェックする際は、XMLタグを除去したプレーンテキストで比較するか、`Contains` で柔軟に判定すること。

### キーワード指定のハマりどころ（最重要）
- `GetTableXmlNearText` のキーワードに **章番号（`1.`, `2.` など）を含めない** こと。
  - 見出しスタイルの自動番号は `<w:numPr>` で表現され、テキストノードとしてXMLに存在しないため、`"2.北海道..."` は永久にヒットしない。
  - 正：`"北海道のうまいもの市場売上"` ／ 誤：`"2.北海道のうまいもの市場売上"`
- 同じ理由で、リスト番号・脚注番号など「自動生成される文字」は検索文字列に含めない。
- 一方、段落内でランが分割されている場合（書式が途中で変わって `<w:r>` が複数になる場合）は `GetTableXmlNearText` 側でタグ除去してから `Contains` するため吸収できる。

### 距離制限
- `GetTableXmlNearText` は見出し段落の直後 **10000 文字以内** の最初の `<w:tbl>` を返す。
  - 当初500文字だったが、間に挟まる段落（例：「単位:千円」）の書式情報量で簡単に500を超えるため拡張した。
  - 文書構造を変える際は念のためこの値の妥当性を再確認すること。

### Ribbon.xml と Ribbon.cs の優先順位（最重要）
- VSTO アドインのリボン UI と idMso フックは **`Ribbon.xml`（リソース）が実体**であり、`Ribbon.cs` 内のハードコード XML はフォールバックに過ぎない。
- **操作ログを使う方針（XML + ログ AND）を採用するタスクでは、必ず以下を事前確認すること**：
  1. 対象の `idMso`（例：`TableColumnsDistribute`、`TableRepeatHeaderRows`）が `Ribbon.xml` の `<commands>` 内に登録されているか。
  2. 登録されていない場合は `<command idMso="..." onAction="CommandOnAction" />` を追加してビルド・再インストール。
  3. ログファイル（`%TEMP%\mos_word_log.txt`）に該当 `idMso` が記録されることを動作確認。
  - `Ribbon.cs` だけ書き換えても XML が読み込まれている限り反映されない点に注意。

### idMso でフックできないコマンドの実例リスト
以下のコマンドは Ribbon.xml に正しく登録（リビルド・再インストール済み）しても、`onAction` が発火せずログに記録されない。**操作ログ方針を採用する前に hookability を確認すること。**

| idMso | 操作 | 判明したプロジェクト |
| :--- | :--- | :--- |
| `TableSplitTable` | 表の分割 | プロジェクト6 |
| `TableSplitCells` | セルの分割 | プロジェクト6 |
| `UpgradeDocument` | 互換モードの「変換」 | プロジェクト7-1 |

**共通の対処**：これらは状態の **ユニークなシグネチャ**（XML/COMプロパティの一意な組み合わせ）で代替する。
- 例：`UpgradeDocument` → `ext == ".doc" && CompatibilityMode == 15` で「変換」直後の状態を一意に検出（7-1）
- 「変換」操作は **ディスク上のファイル名を変えず**、メモリ内のドキュメント形式のみアップグレードするという挙動を活用。

### COM 利用時の安全策
- `Marshal.GetActiveObject("Word.Application")` の取得失敗時に `new Application()` を起動するパターンは避ける（採点中に新規 Word プロセスが立ち上がるリスク）。失敗時は素直に `return false` する。
- COM オブジェクトは取得した順に `Marshal.ReleaseComObject` で解放する。`finally` で `document` を解放するパターンを既存実装に合わせる。
- 可能な限り COM への依存を減らし、`document.WordOpenXML` を取得した後は XML 上で完結させる。

### COM プロパティの値取得：`.Value` 経由が必須（プロジェクト7-2 で判明）
- `Document.BuiltInDocumentProperties["Company"]` などの **`DocumentProperty` COM オブジェクト** を直接 `.ToString()` してはいけない。
  - `.ToString()` は **オブジェクトの型名**（例：`System.__ComObject`）を返すことがあり、実値（例：`"ラビット出版"`）と比較すると**常に false** になる。
  - これは Office/.NET のバージョン依存で **挙動が変わる**ため、たまたま動いていたコードが突然壊れるリスクがある。
- 必ず以下のように `.Value` 経由でアクセスする：

```csharp
dynamic prop = ((dynamic)document.BuiltInDocumentProperties)["Company"];
string company = prop?.Value?.ToString() ?? "";
return company == "ラビット出版";
```

- 同じ作法は `Author`, `Title`, `Subject`, `Keywords`, `Manager` など `BuiltInDocumentProperties` 全般に適用される。
- 既存のリファレンス実装：`PowerPointChecker1_10.cs` でも `.Value` 経由でアクセスしている（プロジェクト共通ルール）。
- **混入経路**：他AIが書いた既存実装に `[...]?.ToString()` が紛れ込んでいた。レビュー時に「`BuiltInDocumentProperties[...]` の直後に `.Value` がない」パターンは要チェック。

### デバッグ手法
- 判定が想定通りにならない場合は、**段階ごとの結果を出力**するデバッグ関数を一時的に仕込む。
  - 出力先：`System.Diagnostics.Debug.WriteLine`（Visual Studio 出力ウィンドウ）+ `C:\temp\<task>_debug.txt`（ファイル）の両方が便利。
  - `System.Windows.Forms.MessageBox` は参照追加が必要なため、上記2系統を推奨。
- 1行目XMLやサブブロックを `C:\temp\xxx.xml` にダンプして、Word側で実際に生成される構造を確認する手法は 6-4 などで有効だった。
- 正解動作後・誤動作後の XML を比較し、差分から判定条件を組み立てる。
- **解決後は必ずデバッグコードを削除**（ログファイルへの追記もコストになるため）。

### 効果系（6-4 系）特有の注意
- 「文字の効果と体裁」の各プリセットは、`<w14:textOutline>` の有無や `<w14:shadow>` 等の有無で互いに区別される。
  - 単に「輪郭がオレンジ」だけで判定すると、別プリセット（影付き等）にも誤判定する。
  - 「輪郭ブロック内に accent2/ED7D31」「塗りブロックも同じ色」「影/反射/光彩なし」のように **タグ位置で限定** すること。
- フォント色のみで accent2 を設定した場合は `<w:color w:themeColor="accent2"/>` になり、`<w14:textOutline>` は生成されない。両者は明確に異なるパターンとして扱うこと。

### MOSの採点ポリシーと許容範囲
- リボンボタン・右クリックメニュー・ダイアログのチェックボックスなど、**同じ結果が得られる操作はすべて正解として許容**するのが基本。
  - 例：6-3 のセル分割は右クリックでも可、6-6 のタイトル行繰り返しは表のプロパティダイアログでも可。
- ただし、6-5 のように「結果は同じだが意図・学習価値が異なる操作（手動で同じ幅にする等）」を区別したい場合は、ログ AND で操作経路を縛る。
- どちらの方針が適切かは「**そのタスクで何を学んでほしいか**」を基準に判断する。

### 既存実装の改修判断
- すでに採点が正しく動作しているタスクは、**形式的なXML化のためだけに改修しない**。
  - 改修によるリグレッションリスクと、改修で得られる安定性向上を比較する。
  - 6-7 のように「対象表が事実上ユニークに絞れる」ケースは現状維持が合理的。
- 改修対象になるのは原則として以下のいずれか：
  1. 現状で × になる／不安定（誤判定が出る）。
  2. COM特有のクラッシュ・リーク・タイムアウトのリスクが顕在化している。
  3. 同様の操作を別タスクでXML化済みで、整合性のために揃えたい（ただし無理に揃えない）。

---

## 結論

プロジェクト6の改修作業は**大成功**に終わりました。
表操作特有の「ログフック不可コマンド」や「手動ズル操作」に対し、OOXMLの構造解析（`gridSpan`, `gridCol`, `tblHeader`等）と動的ログフックの適切な棲み分け（ハイブリッド判定）により、完璧な採点エンジンを構築しました。また、本プロジェクトで蓄積された数々の技術的ナレッジは、後続のすべてのプロジェクトの品質の礎となりました。
