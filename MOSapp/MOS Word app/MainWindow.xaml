<Window x:Class="MOS_Word_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MOS_Word_app"
        mc:Ignorable="d"
        Title="MOS Word App" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Background="#F5F5F5">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Control（右上コントロール配置は Excel と同じ） -->
        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,10,10,0"
                        Panel.ZIndex="100">
                <!-- 採点ボタンの表示 -->
                <CheckBox Content="採点ボタンの表示"
                          IsChecked="{Binding ShowScoreButton, Mode=TwoWay}"
                          Margin="0,0,15,0"
                          VerticalAlignment="Center"
                          FontSize="14"/>
                <!-- 類題ボタンの表示の位置を空ける（Excelのみ） -->
                <FrameworkElement Width="145" Margin="0,0,15,0"/>
                <!-- 全プロジェクトリセットボタン -->
                <Button Content="全プロジェクトリセット"
                        Command="{Binding ResetAllInGroupCommand}"
                        CommandParameter="{Binding CurrentGroupIdForReset}"
                        Margin="0,0,15,0"
                        Padding="15,8"
                        MinWidth="140"
                        FontSize="14"
                        FontWeight="Medium"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#E53935"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Margin="2">
                                            <Border.Effect>
                                                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="1" Opacity="0.2" BlurRadius="2"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C62828"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#B71C1C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Grid Grid.Column="0" Grid.ColumnSpan="2">
        <TabControl SelectedIndex="{Binding SelectedTabIndex}" Background="#F0F0F3">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="#E3F2FD" BorderBrush="#90CAF9" BorderThickness="2,2,2,0" CornerRadius="15,15,0,0" Margin="4,0" Padding="20,12">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#0D47A1" Direction="135" ShadowDepth="4" Opacity="0.3" BlurRadius="8"/>
                                    </Border.Effect>
                                    <Border CornerRadius="15,15,0,0" Margin="1">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" Opacity="0.8" BlurRadius="6"/>
                                        </Border.Effect>
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" TextElement.FontWeight="SemiBold" TextElement.FontSize="14" TextElement.Foreground="#1A1A1A"/>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1976D2" Offset="0"/>
                                                    <GradientStop Color="#42A5F5" Offset="0.5"/>
                                                    <GradientStop Color="#64B5F6" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#1565C0"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#FFFFFF"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                        <Setter TargetName="Border" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#0D47A1" Direction="135" ShadowDepth="6" Opacity="0.4" BlurRadius="10"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="演習">
                <ScrollViewer Background="#F0F0F3">
                    <Grid Margin="20">
                        <ItemsControl ItemsSource="{Binding ProjectGroups[0].Projects}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="5" Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                            Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Margin="12" Padding="20,15" MinWidth="140" MinHeight="60">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#1976D2" Offset="0"/>
                                                            <GradientStop Color="#42A5F5" Offset="0.5"/>
                                                            <GradientStop Color="#64B5F6" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20"
                                                                    Margin="4">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#0D47A1" Direction="135" ShadowDepth="8" Opacity="0.4" BlurRadius="15"/>
                                                                </Border.Effect>
                                                                <Border CornerRadius="20" Margin="2">
                                                                    <Border.Effect>
                                                                        <DropShadowEffect Color="#BBDEFB" Direction="315" ShadowDepth="6" Opacity="0.8" BlurRadius="12"/>
                                                                    </Border.Effect>
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#1E88E5" Offset="0"/>
                                                                    <GradientStop Color="#5C9CE5" Offset="0.5"/>
                                                                    <GradientStop Color="#7AB3F5" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#1565C0" Offset="0"/>
                                                                    <GradientStop Color="#3A7BC8" Offset="0.5"/>
                                                                    <GradientStop Color="#5A9AE5" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
            </Grid>
        </Grid>

        <!-- Result Display -->
        <Border Grid.Row="1" Margin="20,10" Background="#FFFFFF" BorderBrush="#A0A0A0" BorderThickness="2" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3" BlurRadius="5"/>
            </Border.Effect>
            <TextBlock Text="{Binding ResultMessage}" Margin="15" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="40"/>
        </Border>
    </Grid>
</Window>
