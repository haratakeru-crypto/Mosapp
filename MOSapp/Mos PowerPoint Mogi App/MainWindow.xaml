<Window x:Class="MOS_PowerPoint_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MOS_PowerPoint_app"
        mc:Ignorable="d"
        Title="MOS PowerPoint App" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- オレンジのヘッダーバー -->
        <Border Grid.Row="0" Background="#D04423" Height="50">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左側: UIテストボタン -->
                <Button Grid.Column="0" 
                        Content="UIテスト" 
                        Command="{Binding UiTestCommand}"
                        HorizontalAlignment="Left"
                        Margin="10,5"
                        Padding="20,8"
                        Background="#FF6B35"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- 右側: 採点ボタン、タイマー設定、テストモードテキスト -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <!-- 採点ボタン -->
                    <Button Content="採点" 
                            Command="{Binding ScoreCommand}"
                            Margin="0,0,15,0"
                            Padding="20,8"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            MinWidth="80">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="5"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- タイマー有効/無効チェックボックス -->
                    <CheckBox Name="TimerCheckBox" 
                              Content="タイマーを使用する"
                              IsChecked="True"
                              Foreground="White"
                              FontSize="14"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"
                              Checked="TimerCheckBox_Checked"
                              Unchecked="TimerCheckBox_Unchecked">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="CheckBox">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" 
                                                        Width="18" 
                                                        Height="18" 
                                                        BorderBrush="White" 
                                                        BorderThickness="2" 
                                                        Background="Transparent"
                                                        CornerRadius="2"
                                                        Margin="0,0,8,0">
                                                    <TextBlock Text="✓" 
                                                               FontSize="12" 
                                                               Foreground="White" 
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=CheckBox}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                </Border>
                                                <ContentPresenter Grid.Column="1" 
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                    
                    <TextBlock Text="テストモード" 
                              Foreground="White"
                              VerticalAlignment="Center"
                              FontSize="14"
                              FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="1" Margin="20" SelectedIndex="{Binding SelectedTabIndex}" Background="#F0F0F3">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="#FFE5D9" BorderBrush="#FF8C65" BorderThickness="2,2,2,0" CornerRadius="15,15,0,0" Margin="4,0" Padding="20,12">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="4" Opacity="0.3" BlurRadius="8"/>
                                    </Border.Effect>
                                    <Border CornerRadius="15,15,0,0" Margin="1">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" Opacity="0.8" BlurRadius="6"/>
                                        </Border.Effect>
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" TextElement.FontWeight="SemiBold" TextElement.FontSize="14" TextElement.Foreground="#1A1A1A"/>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#D04423" Offset="0"/>
                                                    <GradientStop Color="#FF6B35" Offset="0.5"/>
                                                    <GradientStop Color="#FF8C65" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#B83A1A"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#FFFFFF"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                        <Setter TargetName="Border" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="6" Opacity="0.4" BlurRadius="10"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="演習">
                <ScrollViewer Background="#F0F0F3">
                    <Grid Margin="20">
                        <ItemsControl ItemsSource="{Binding ProjectGroups[0].Projects}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="6" Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                            Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Margin="12" Padding="20,15" MinWidth="140" MinHeight="60">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#D04423" Offset="0"/>
                                                            <GradientStop Color="#FF6B35" Offset="0.5"/>
                                                            <GradientStop Color="#FF8C65" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20"
                                                                    Margin="4">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="8" Opacity="0.4" BlurRadius="15"/>
                                                                </Border.Effect>
                                                                <Border CornerRadius="20" Margin="2">
                                                                    <Border.Effect>
                                                                        <DropShadowEffect Color="#FFD4C4" Direction="315" ShadowDepth="6" Opacity="0.8" BlurRadius="12"/>
                                                                    </Border.Effect>
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#E04F2A" Offset="0"/>
                                                                    <GradientStop Color="#FF7A4A" Offset="0.5"/>
                                                                    <GradientStop Color="#FF9D75" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#B83A1A" Offset="0"/>
                                                                    <GradientStop Color="#D85A2F" Offset="0.5"/>
                                                                    <GradientStop Color="#F07A4A" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="応用編">
                <ScrollViewer Background="#F0F0F3">
                    <Grid Margin="20">
                        <ItemsControl ItemsSource="{Binding ProjectGroups[1].Projects}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="6" Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                            Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Margin="12" Padding="20,15" MinWidth="140" MinHeight="60">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#D04423" Offset="0"/>
                                                            <GradientStop Color="#FF6B35" Offset="0.5"/>
                                                            <GradientStop Color="#FF8C65" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20"
                                                                    Margin="4">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="8" Opacity="0.4" BlurRadius="15"/>
                                                                </Border.Effect>
                                                                <Border CornerRadius="20" Margin="2">
                                                                    <Border.Effect>
                                                                        <DropShadowEffect Color="#FFD4C4" Direction="315" ShadowDepth="6" Opacity="0.8" BlurRadius="12"/>
                                                                    </Border.Effect>
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#E04F2A" Offset="0"/>
                                                                    <GradientStop Color="#FF7A4A" Offset="0.5"/>
                                                                    <GradientStop Color="#FF9D75" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#B83A1A" Offset="0"/>
                                                                    <GradientStop Color="#D85A2F" Offset="0.5"/>
                                                                    <GradientStop Color="#F07A4A" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Result Display -->
        <Border Grid.Row="2" Margin="20,10" Background="#FFFFFF" BorderBrush="#A0A0A0" BorderThickness="2" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3" BlurRadius="5"/>
            </Border.Effect>
            <TextBlock Text="{Binding ResultMessage}" Margin="15" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="40"/>
        </Border>
    </Grid>
</Window>
