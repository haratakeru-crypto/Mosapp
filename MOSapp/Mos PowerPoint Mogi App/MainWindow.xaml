<Window x:Class="MOS_PowerPoint_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MOS_PowerPoint_app"
        mc:Ignorable="d"
        Title="MOS PowerPoint App" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Control（右上コントロール配置は Excel と同じ） -->
        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,10,10,0"
                        Panel.ZIndex="100">
                <!-- 採点ボタンの表示 -->
                <CheckBox Content="採点ボタンの表示"
                          IsChecked="{Binding ShowScoreButton, Mode=TwoWay}"
                          Margin="0,0,15,0"
                          VerticalAlignment="Center"
                          FontSize="14"/>
                <!-- 類題ボタンの表示の位置を空ける（Excelのみ） -->
                <FrameworkElement Width="145" Margin="0,0,15,0"/>
                <!-- 全プロジェクトリセットボタン -->
                <Button Content="全プロジェクトリセット"
                        Command="{Binding ResetAllProjectsCommand}"
                        Margin="0,0,15,0"
                        Padding="15,8"
                        MinWidth="140"
                        FontSize="14"
                        FontWeight="Medium"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#F59E0B"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Margin="2">
                                            <Border.Effect>
                                                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="1" Opacity="0.2" BlurRadius="2"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D97706"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#B45309"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Grid Grid.Column="0" Grid.ColumnSpan="2">
        <TabControl SelectedIndex="{Binding SelectedTabIndex}" Background="#F0F0F3">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="#FFE5D9" BorderBrush="#FF8C65" BorderThickness="2,2,2,0" CornerRadius="15,15,0,0" Margin="4,0" Padding="20,12">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="4" Opacity="0.3" BlurRadius="8"/>
                                    </Border.Effect>
                                    <Border CornerRadius="15,15,0,0" Margin="1">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" Opacity="0.8" BlurRadius="6"/>
                                        </Border.Effect>
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" TextElement.FontWeight="SemiBold" TextElement.FontSize="14" TextElement.Foreground="#1A1A1A"/>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#D04423" Offset="0"/>
                                                    <GradientStop Color="#FF6B35" Offset="0.5"/>
                                                    <GradientStop Color="#FF8C65" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#B83A1A"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#FFFFFF"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                        <Setter TargetName="Border" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="6" Opacity="0.4" BlurRadius="10"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="演習">
                <ScrollViewer Background="#F0F0F3">
                    <Grid Margin="20">
                        <ItemsControl ItemsSource="{Binding ProjectGroups[0].Projects}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="6" Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                            Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Margin="12" Padding="20,15" MinWidth="140" MinHeight="60">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#D04423" Offset="0"/>
                                                            <GradientStop Color="#FF6B35" Offset="0.5"/>
                                                            <GradientStop Color="#FF8C65" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20"
                                                                    Margin="4">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#8B2E16" Direction="135" ShadowDepth="8" Opacity="0.4" BlurRadius="15"/>
                                                                </Border.Effect>
                                                                <Border CornerRadius="20" Margin="2">
                                                                    <Border.Effect>
                                                                        <DropShadowEffect Color="#FFD4C4" Direction="315" ShadowDepth="6" Opacity="0.8" BlurRadius="12"/>
                                                                    </Border.Effect>
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#E04F2A" Offset="0"/>
                                                                    <GradientStop Color="#FF7A4A" Offset="0.5"/>
                                                                    <GradientStop Color="#FF9D75" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#B83A1A" Offset="0"/>
                                                                    <GradientStop Color="#D85A2F" Offset="0.5"/>
                                                                    <GradientStop Color="#F07A4A" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
            </Grid>
        </Grid>

        <!-- Result Display -->
        <Border Grid.Row="1" Margin="20,10" Background="#FFFFFF" BorderBrush="#A0A0A0" BorderThickness="2" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3" BlurRadius="5"/>
            </Border.Effect>
            <TextBlock Text="{Binding ResultMessage}" Margin="15" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="40"/>
        </Border>
    </Grid>
</Window>
