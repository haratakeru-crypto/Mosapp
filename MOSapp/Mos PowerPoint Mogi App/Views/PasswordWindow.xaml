<Window x:Class="MOS_PowerPoint_app.Views.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="パスワード入力"
        Width="320"
        Height="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="パスワードを入力してください" 
                   FontSize="14" Foreground="#374151"
                   Margin="0,0,0,15"/>
        
        <PasswordBox Grid.Row="1" x:Name="PasswordBox"
                     FontSize="14" Padding="8,6"
                     Margin="0,0,0,20"
                     PasswordChanged="PasswordBox_PasswordChanged"/>
        
        <TextBlock Grid.Row="2" x:Name="ErrorTextBlock"
                   Text="" Foreground="#DC2626" FontSize="12"
                   Visibility="Collapsed"
                   VerticalAlignment="Top"
                   Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Name="OkButton"
                    Width="80" Height="32"
                    Background="#1E40AF" Foreground="White"
                    BorderThickness="0"
                    IsDefault="True"
                    Click="OkButton_Click"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="キャンセル" Name="CancelButton"
                    Width="80" Height="32"
                    Background="#6B7280" Foreground="White"
                    BorderThickness="0"
                    IsCancel="True"
                    Click="CancelButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
