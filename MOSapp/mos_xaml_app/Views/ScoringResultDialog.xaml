<Window x:Class="MOSExcelMogiApp.Views.ScoringResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="採点結果" Height="250" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="採点結果" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10"/>
        
        <!-- Task Numbers Row -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Task" FontWeight="Bold" Width="60" TextAlignment="Center" Margin="5"/>
            <ItemsControl x:Name="TaskNumbersControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Width="40" TextAlignment="Center" 
                                   FontWeight="Bold" Margin="5" FontSize="14"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        
        <!-- Results Row -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Result" FontWeight="Bold" Width="60" TextAlignment="Center" Margin="5"/>
            <ItemsControl x:Name="ResultsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="40" Height="40" Margin="5">
                            <TextBlock Text="{Binding Text}" 
                                       Width="40" Height="40" 
                                       TextAlignment="Center" 
                                       VerticalAlignment="Center"
                                       Foreground="{Binding Color}" 
                                       FontWeight="Bold" 
                                       FontSize="14"
                                       PreviewMouseDown="ResultItem_MouseDown">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsClickable}" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                                <Setter Property="TextDecorations" Value="Underline"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        
        <Button Grid.Row="4" Content="閉じる" Width="100" Height="30" 
                HorizontalAlignment="Center" Margin="10" Click="CloseButton_Click"/>
    </Grid>
</Window>