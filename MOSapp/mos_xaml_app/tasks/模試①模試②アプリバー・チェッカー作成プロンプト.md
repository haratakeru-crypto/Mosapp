# 模試①・模試②アプリバー・チェッカー作成プロンプト

## 概要
現在のMOS_XAML_APP（Excel採点アプリ）に、模試①（GroupId=2）と模試②（GroupId=3）のアプリバーと正誤判定チェッカーを追加してください。

**重要**: この作業は**同じCursor、同じフォルダ**で行います。既存のExcel版アプリに機能を追加する形になります。

## 既存の実装状況

### 現在の構成
- **演習（GroupId=1）**: 既に実装済み
  - JSONファイル: `MOS演習問題文一覧.json`
  - Checkerクラス: `ExcelChecker1_1.cs` ～ `ExcelChecker1_10.cs`
  - config.json: `tabs["1"]`

- **模試①（GroupId=2）**: 部分実装済み
  - JSONファイル: `MOS模擬試験①問題文一覧.json`（既に存在）
  - Checkerクラス: `ExcelChecker2_1.cs` ～ `ExcelChecker2_10.cs`（既に存在、実装は後程）
  - config.json: `tabs["2"]`（既に存在）
  - アプリバー: 既存の`AppBarWindow`がGroupId=2に対応済み

- **模試②（GroupId=3）**: 部分実装済み
  - JSONファイル: `MOS模擬試験②問題文一覧.json`（既に存在）
  - Checkerクラス: `ExcelChecker3_1.cs` ～ `ExcelChecker3_10.cs`（既に存在、実装は後程）
  - config.json: `tabs["3"]`（既に存在）
  - アプリバー: 既存の`AppBarWindow`がGroupId=3に対応済み

## 同じUI・機能（演習版から継承）

### アプリバー（AppBarWindow）
- **配置**: 画面下部に固定表示（1920x258サイズ、Y=774位置）
- **ヘッダーバー（上部）**:
  - 左側: タイマー表示（🕐 50:00:00形式）、レビューページボタン/結果画面に戻るボタン
  - 中央: プロジェクト情報表示（例: "プロジェクト 1"）
  - 右側: 採点ボタン、一時停止ボタン、リセットボタン、次のプロジェクトボタン、アイコンボタン（↓、□）
- **メインコンテンツエリア（下部）**:
  - ナビゲーション: 前へボタン（←）、タスク番号ボタン（実際の問題数に合わせる）、次へボタン（→）
  - 問題文表示エリア: 現在のタスクの問題文を表示（"で囲まれた部分に下線を付けて表示、クリックでクリップボードにコピー）
  - アクションボタン: 解答済みボタン、あとで見直すボタン（フラグ）

**重要**: アプリバーのボタン配置と機能は、演習版と**完全に同じ**です。変更は不要です。

## 確認が必要な箇所

### 1. アプリバーの問題文読み込み（CSVファイルから読み込む実装済み）
**現在の実装（AppBarWindow.xaml.cs）**:
- **模試①（GroupId=2）**: CSVファイルから読み込む
  - CSVファイル: `References/CSV/解答手順あり模擬試験①問題文.csv`
  - 形式: `グループ,プロジェクト,問題文,解答操作`
  - `LoadTasksFromCsv()`メソッドで読み込み
- **その他のGroupId**: JSONファイルから読み込む
  ```csharp
  string jsonFileName = groupId switch
  {
      1 => "MOS演習問題文一覧.json",        // GroupId=1 → 演習タブ
      2 => "MOS模擬試験①問題文一覧.json",  // GroupId=2 → 模試①タブ（通常はCSVから読み込むが、フォールバック用）
      3 => "MOS模擬試験②問題文一覧.json",  // GroupId=3 → 模試②タブ
      _ => "MOS模擬アプリ問題文一覧.json"
  };
  ```

**CSVファイルから問題文を読み込む実装**:
```csharp
private void LoadTasksFromCsv(int groupId)
{
    // CSVファイルのパス
    string csvPath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "References", "CSV", "解答手順あり模擬試験①問題文.csv");
    
    // CSVファイルを読み込む
    var projects = new Dictionary<int, List<TaskInfo>>();
    string[] lines = File.ReadAllLines(csvPath, Encoding.UTF8);
    
    // ヘッダー行をスキップ（1行目）
    for (int i = 1; i < lines.Length; i++)
    {
        string line = lines[i].Trim();
        if (string.IsNullOrEmpty(line))
            continue;
        
        // CSVのパース（カンマ区切り、引用符内のカンマを考慮）
        string[] fields = ParseCsvLine(line);
        if (fields.Length < 3)
            continue;
        
        // グループ,プロジェクト,問題文,解答操作
        if (int.TryParse(fields[0].Trim(), out int csvGroupId) && 
            int.TryParse(fields[1].Trim(), out int projectId) &&
            csvGroupId == groupId)
        {
            string description = fields[2].Trim();
            if (string.IsNullOrEmpty(description))
                continue;
            
            if (!projects.ContainsKey(projectId))
            {
                projects[projectId] = new List<TaskInfo>();
            }
            
            int taskId = projects[projectId].Count + 1;
            projects[projectId].Add(new TaskInfo
            {
                TaskId = taskId,
                Description = description
            });
        }
    }
    
    // ProjectData形式に変換して、_projectDataに設定
    // 現在のプロジェクトのタスクを取得して、_tasksに設定
}
```

**CSVパーサー（引用符内のカンマに対応）**:
```csharp
private string[] ParseCsvLine(string line)
{
    var fields = new List<string>();
    bool inQuotes = false;
    StringBuilder currentField = new StringBuilder();
    
    for (int i = 0; i < line.Length; i++)
    {
        char c = line[i];
        
        if (c == '"')
        {
            if (inQuotes && i + 1 < line.Length && line[i + 1] == '"')
            {
                // エスケープされた引用符
                currentField.Append('"');
                i++; // 次の文字をスキップ
            }
            else
            {
                // 引用符の開始/終了
                inQuotes = !inQuotes;
            }
        }
        else if (c == ',' && !inQuotes)
        {
            // フィールドの区切り
            fields.Add(currentField.ToString());
            currentField.Clear();
        }
        else
        {
            currentField.Append(c);
        }
    }
    
    // 最後のフィールドを追加
    fields.Add(currentField.ToString());
    
    return fields.ToArray();
}
```

**確認事項**: 
- `References/CSV/解答手順あり模擬試験①問題文.csv` が存在することを確認
- CSVファイルの構造が正しいことを確認（`グループ,プロジェクト,問題文,解答操作`）
- `.csproj`にCSVファイルのコピー設定が追加されていることを確認

### 2. レビュー画面の問題文読み込み（CSVファイルから読み込む実装済み）
**現在の実装（ReviewPageWindow.xaml.cs）**:
- **模試①（GroupId=2）**: CSVファイルから読み込む
  - `LoadProjectsFromCsv()`メソッドで読み込み
- **その他のGroupId**: JSONファイルから読み込む
  ```csharp
  string jsonFileName = _groupId switch
  {
      1 => "MOS演習問題文一覧.json",        // GroupId=1 → 演習タブ
      2 => "MOS模擬試験①問題文一覧.json",  // GroupId=2 → 模試①タブ（通常はCSVから読み込むが、フォールバック用）
      3 => "MOS模擬試験②問題文一覧.json",  // GroupId=3 → 模試②タブ
      _ => "MOS模擬アプリ問題文一覧.json"
  };
  ```

**確認事項**: レビュー画面も既にGroupId=2の場合はCSVファイルから読み込む実装が完了しています。

### 3. 問題数の管理（CSVファイル）
各プロジェクトの問題数は、CSVファイルで管理されます。

**CSVファイルの形式**:
```
GroupId,ProjectId,TaskCount
1,1,7
1,2,5
2,1,7
2,2,5
3,1,5
3,2,5
...
```

**実装方法**:
- CSVファイルを`References/CSV/`フォルダに配置（例: `Excel問題数一覧.csv`）
- アプリ起動時またはプロジェクト選択時にCSVファイルから問題数を読み込む
- `config.json`の`taskCount`を動的に設定するか、CSVファイルを直接参照する実装にする
- アプリバーのタスク番号ボタンは、読み込んだ問題数に基づいて動的に表示する（既存の実装で対応済み）

**確認事項**:
- 模試①（GroupId=2）と模試②（GroupId=3）の各プロジェクトの問題数がCSVファイルに含まれていることを確認
- CSVファイルから問題数を読み込む実装が既に存在する場合は、GroupId=2とGroupId=3に対応していることを確認

### 4. config.jsonの設定（既に存在）
**現在のconfig.json**:
```json
{
  "tabs": {
    "1": {
      "projects": {
        "1": {
          "taskCount": 7,
          "library": "ExcelChecker1_1",
          "excelFile": "C:\\MOSTest\\Excel365\\Tab1\\project1.xlsx"
        }
      }
    },
    "2": {
      "projects": {
        "1": {
          "taskCount": 7,
          "library": "ExcelChecker2_1",
          "excelFile": "C:\\MOSTest\\Excel365\\Tab2\\project1.xlsx"
        }
      }
    },
    "3": {
      "projects": {
        "1": {
          "taskCount": 5,
          "library": "ExcelChecker3_1",
          "excelFile": "C:\\MOSTest\\Excel365\\Tab3\\project1.xlsx"
        }
      }
    }
  }
}
```

**確認事項**:
- `tabs["2"]`（模試①）と`tabs["3"]`（模試②）の設定が正しいことを確認
- 各プロジェクトの`taskCount`が実際の問題数と一致していることを確認（CSVファイルと照合）
- 各プロジェクトの`excelFile`パスが正しいことを確認

## 実装が必要な箇所

### 1. Checkerクラスの実装（既存のExcelCheckerを参考に作成）
**模試①（GroupId=2）のCheckerクラス**:
- `Libraries/Group2/ExcelChecker2_1.cs` ～ `ExcelChecker2_10.cs`（既に存在）
- チェックメソッド名: `CheckTask_2_1_01()`, `CheckTask_2_1_02()` など
- 命名規則: `CheckTask_{GroupId}_{ProjectId}_{TaskId:00}`

**模試②（GroupId=3）のCheckerクラス**:
- `Libraries/Group3/ExcelChecker3_1.cs` ～ `ExcelChecker3_10.cs`（または`ExcelChecker3_11.cs`まで）（既に存在）
- チェックメソッド名: `CheckTask_3_1_01()`, `CheckTask_3_1_02()` など
- 命名規則: `CheckTask_{GroupId}_{ProjectId}_{TaskId:00}`

**既存のExcelCheckerを参考にする方法**:
既存の`Libraries/Group1/ExcelChecker1_*.cs`を参考にして、模試①・模試②のCheckerを作成できます。同じ操作パターンが多く、チェックロジックを再利用できます。

**参考にできる操作パターン**:
1. **印刷関連の操作**
   - 印刷の向きを横向きにする → `ExcelChecker1_1.cs`の`CheckTask_1_1_01()`を参考
   - 印刷範囲の設定 → 同様のパターン
   - 印刷タイトルの設定 → 同様のパターン

2. **テーブル関連の操作**
   - テーブルスタイルの設定 → `ExcelChecker1_2.cs`の`CheckTask_1_2_03()`を参考
   - 縞模様の設定 → `CheckTask_1_2_01()`を参考
   - 最後の列の強調 → `CheckTask_1_2_02()`を参考

3. **グラフ関連の操作**
   - グラフの作成・設定 → 他のCheckerファイルを参考
   - スパークラインの設定 → `ExcelChecker1_4.cs`を参考

4. **その他の操作**
   - 条件付き書式 → 他のCheckerファイルを参考
   - 関数の使用 → 他のCheckerファイルを参考

**実装手順**:
1. CSVファイル（`References/CSV/模擬試験①問題文.csv`、`References/CSV/模擬試験②問題文.csv`）を確認
2. 各問題文に対応する既存のCheckerメソッドを探す
3. 既存のCheckerメソッドをコピーして、シート名やセル範囲を変更
4. 必要に応じて、チェックロジックを調整

**実装例**:
模試①のプロジェクト1のタスク1「『売上一覧』シートの印刷の向きを横向きにします。」は、`ExcelChecker1_1.cs`の`CheckTask_1_1_01()`を参考に、シート名を「売上一覧」に変更するだけで実装できます。

```csharp
private bool CheckTask_2_1_01_Impl(string filePath)
{
    Application excelApp = null;
    Workbook workbook = null;
    Worksheet worksheet = null;
    try
    {
        // 既に開いているExcelアプリケーションを取得
        try
        {
            excelApp = (Application)Marshal.GetActiveObject("Excel.Application");
        }
        catch
        {
            excelApp = new Application();
            excelApp.Visible = true;
        }
        
        workbook = null;
        string fileName = System.IO.Path.GetFileName(filePath);
        
        foreach (Workbook wb in excelApp.Workbooks)
        {
            if (wb.FullName.Equals(filePath, StringComparison.OrdinalIgnoreCase) ||
                wb.Name.Equals(fileName, StringComparison.OrdinalIgnoreCase))
            {
                workbook = wb;
                break;
            }
        }
        
        if (workbook == null) return false;
        
        // 売上一覧シートを検索（ExcelChecker1_1.csから変更）
        worksheet = FindWorksheet(workbook, "売上一覧");
        if (worksheet == null)
        {
            return false;
        }
        
        // 印刷方向をチェック
        XlPageOrientation orientation = worksheet.PageSetup.Orientation;
        
        return orientation == XlPageOrientation.xlLandscape;
    }
    catch (Exception)
    {
        return false;
    }
    finally
    {
        if (worksheet != null) Marshal.ReleaseComObject(worksheet);
    }
}
```

**注意**: 既存のCheckerクラスファイルは存在しますが、CSVファイルの問題文と照らし合わせて、実装が正しいか確認し、不足している部分があれば追加してください。

### 2. Checkerクラスの呼び出し（既に実装済みの可能性）
**現在の実装確認**:
- `ExcelCheckerService.cs`や`MainViewModel.cs`で、GroupIdに基づいてCheckerクラスを呼び出しているか確認
- 模試①（GroupId=2）と模試②（GroupId=3）に対応しているか確認

**実装例**:
```csharp
string libraryName = $"ExcelChecker{groupId}_{projectId}";
string dllPath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "Libraries", GetGroupFolder(libraryName), $"{libraryName}.dll");
Type checkerType = assembly.GetType(libraryName);
```

## 実装手順

1. **既存実装の確認**
   - `AppBarWindow.xaml.cs`の`LoadTasks()`メソッドで、GroupId=2とGroupId=3に対応していることを確認
   - `ReviewPageWindow.xaml.cs`で、GroupId=2とGroupId=3に対応していることを確認
   - `config.json`に`tabs["2"]`と`tabs["3"]`が存在することを確認

2. **JSONファイルの確認**
   - `References/JSON/MOS模擬試験①問題文一覧.json` が存在し、正しい構造であることを確認
   - `References/JSON/MOS模擬試験②問題文一覧.json` が存在し、正しい構造であることを確認
   - 各JSONファイルに、対応するプロジェクトの問題文が含まれていることを確認

3. **CSVファイルの準備**
   - `References/CSV/解答手順あり模擬試験①問題文.csv` が存在することを確認
   - CSVファイルの形式: `グループ,プロジェクト,問題文,解答操作`
   - 模試①（GroupId=2）の各プロジェクトの問題文がCSVファイルに含まれていることを確認
   - `.csproj`にCSVファイルのコピー設定が追加されていることを確認
   ```xml
   <Content Include="References\CSV\解答手順あり模擬試験①問題文.csv">
     <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
   </Content>
   ```

4. **Checkerクラスの確認**
   - `Libraries/Group2/ExcelChecker2_1.cs` ～ `ExcelChecker2_10.cs` が存在することを確認
   - `Libraries/Group3/ExcelChecker3_1.cs` ～ `ExcelChecker3_10.cs`（または`ExcelChecker3_11.cs`まで）が存在することを確認
   - Checkerクラスの実装は後程作成するため、現時点ではクラスファイルの存在確認のみ

5. **動作確認**
   - 模試①タブからプロジェクトを選択してアプリバーが表示されることを確認
   - 模試②タブからプロジェクトを選択してアプリバーが表示されることを確認
   - アプリバーに問題文が正しく表示されることを確認
   - タスク番号ボタンが実際の問題数に合わせて表示されることを確認
   - レビューページで模試①と模試②の問題文が正しく表示されることを確認

## 注意事項

- **アプリバーのボタン配置と機能は演習版と完全に同じ**です。変更は不要です。
- **問題文と問題数は異なります**。模試①（GroupId=2）はCSVファイルから、その他はJSONファイルから読み込みます。
- **CSVファイルのコピー設定**: `.csproj`にCSVファイルのコピー設定を追加してください。これにより、ビルド時に`bin\Debug\References\CSV\`にCSVファイルがコピーされます。
- **プロジェクト変更時の処理**: `OnCurrentProjectChanged`メソッドで、プロジェクトが変更されたときに`LoadTasks()`が呼ばれ、問題文が正しく更新されることを確認してください。
- **正誤判定チェッカーは後程作成**します。現時点では、Checkerクラスのファイルが存在することと、呼び出しロジックが正しいことを確認してください。
- 既存の実装でGroupId=2とGroupId=3に対応している箇所が多いため、**追加実装は最小限**で済む可能性が高いです。
- 模試②（GroupId=3）は、プロジェクト数が11個の場合があります（`ExcelChecker3_11.cs`）。config.jsonとCSVファイルで確認してください。

## 確認チェックリスト

- [ ] `References/JSON/MOS模擬試験①問題文一覧.json` が存在し、正しい構造である
- [ ] `References/JSON/MOS模擬試験②問題文一覧.json` が存在し、正しい構造である
- [ ] `config.json`に`tabs["2"]`と`tabs["3"]`が存在し、設定が正しい
- [ ] CSVファイルに模試①と模試②の各プロジェクトの問題数が記載されている
- [ ] `AppBarWindow.xaml.cs`の`LoadTasks()`がGroupId=2とGroupId=3に対応している
- [ ] `ReviewPageWindow.xaml.cs`がGroupId=2とGroupId=3に対応している
- [ ] `Libraries/Group2/ExcelChecker2_*.cs` が存在する（実装は後程）
- [ ] `Libraries/Group3/ExcelChecker3_*.cs` が存在する（実装は後程）
- [ ] 模試①タブからプロジェクトを選択してアプリバーが正しく表示される
- [ ] 模試②タブからプロジェクトを選択してアプリバーが正しく表示される
- [ ] アプリバーに問題文が正しく表示される
- [ ] タスク番号ボタンが実際の問題数に合わせて表示される

